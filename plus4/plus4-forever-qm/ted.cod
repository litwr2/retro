TED:            INC     TOTALTICKS_LO

                INC     TICKS
                CMP     VIS,3
                JNZ     TED_L1

                JMP     [TED_IDJ]
TED_L22Z:       CMP     TED_1E,SHRA
                JB      TED_L22
                CMP     TED_1E,EHRA
                JA      TED_L22

                PUSH    DS
                POP     ES
                MOV     SI,CRP
                SUB     SI,CRP_S

                CMP     BADLICOND,0
                JNE     SHORT TED_L12

                MOV     AX,WORD PTR TED_1C
                XOR     AL,SST
                AND     AX,7
                CMP     AX,Y_OFFSET
                JNZ     SHORT TED_L2

                MOV     VSA,AH
                AND     VMODE,0BFH
                INC     BADLICOND

TED_L12:        MOV     BX,CRP
                ADD     BH,VM_BASE
                MOV     AH,FS:[BX]
                MOV     AL,FS:[BX+400H]
                MOV     VID_BUF[ESI*2],AX
                DEC     TICKS

TED_L2:         LGS     BX,MDATA_ADDR
                MOV     DX,VID_BUF[ESI*2]       ;DH-ATTR, DL-CHAR
                MOV     SI,VMODE
                MOV     DI,VD_PTR_FILL
                CALL    VMJT[SI]
                MOV     VD_PTR_FILL,DI

                INC     CRP
                LES     DI,BEAM_ADDR
                MOV     AX,WORD PTR TED_1C
                ADD     BEAM_POS,8
                CMP     AX,EVSA
                JA      SHORT TED_L5
                CMP     AX,SVSA
                JB      SHORT TED_L5
                MOV     AL,TED_1E
                CMP     AL,EHSA
                JA      SHORT TED_L5
                CMP     AL,SHSA
                JB      SHORT TED_L5
                CMP     SST2,0
                JNZ     SHORT TED_L5
                MOV     SI,VD_PTR_BEAM
                MOVSD
                MOVSD
                MOV     VD_PTR_BEAM,SI
                JMP     SHORT TED_L7
TED_L5:         MOV     EAX,BORDER_COLOR
                STOSD
                STOSD
                ADD     VD_PTR_BEAM,8
                JMP     SHORT TED_L7

TED_L22:        LES     DI,BEAM_ADDR
                MOV     AX,WORD PTR TED_1C
                ADD     BEAM_POS,8
                CMP     AX,EVSA
                JA      SHORT TED_L5A
                CMP     AX,SVSA
                JB      SHORT TED_L5A
                MOV     AL,TED_1E
                CMP     AL,EHSA
                JA      SHORT TED_L5A
                CMP     AL,SHSA
                JB      SHORT TED_L5A
                CMP     SST2,0
                JNZ     SHORT TED_L5A
                STOSD
                STOSD
                JMP     SHORT TED_L7
TED_L5A:        MOV     EAX,BORDER_COLOR
                STOSD
                STOSD
TED_L1:         TEST    TED_13,2        ;SINGLE CLOCK? (1-ON)
                JNZ     SHORT TED_L7
                INC     TICKS

TED_L7:         CMP     XZN,2
                JA      SHORT TED_L9
                JZ      SHORT TED_L8A
                DEC     XZ1C
                JNZ     SHORT TED_L9

                LEA     DX,TED_L22
                CMP     WORD PTR TED_1C,EVRA
                JA      TED_L22X
                MOV     AX,Y_OFFSET
                MOV     BX,WORD PTR TED_1C
                XOR     BL,SST
                CMP     AH,SST
                JZ      SHORT TED_L22B
                CMP     AH,SST2
                JZ      SHORT TED_L22A
                AND     BL,7
TED_L22B:       CMP     AX,BX
                JA      TED_L22X
TED_L22A:       LEA     DX,TED_L22Z
TED_L22X:       MOV     TED_IDJ,DX

                MOV     AX,CRP_S
                MOV     CRP,AX
                INC     XZN
                INC     VIS
                JMP     SHORT TED_L9
TED_L8A:        DEC     XZ2C
                JNZ     SHORT TED_L9
                INC     XZN
                AND     VIS,0FEH

TED_L9:         TEST    TED_7,20H               ;FREEZE (1-ON)
                JNZ     TED_L11
                INC     TED_1E
                CMP     TED_1E,MAX_HOR+1        ;H-SYNC
                JB      TED_L12A
                MOV     TED_1E,0

                MOV     EAX,TOTALTICKS_LO
                CMP     EAX,TOTALTICKS_O
                JAE     SHORT TED_L32
                INC     TOTALTICKS_HI
TED_L32:        MOV     TOTALTICKS_O,EAX

                CMP     BEAM_POS,BEAM_CH_POS
                JB      SHORT TED_L34
                SUB     BEAM_POS,BEAM_CH_POS
                ADD     BEAM_POS+2,BEAM_CH_POS/16
TED_L34:        XOR     AX,AX
                XCHG    AX,BEAM_POSX
                ADD     BEAM_POS,AX

                LEA     AX,VIDEO_DATA
                MOV     VD_PTR_BEAM,AX
                ADD     AX,X_OFFSET
                MOV     VD_PTR_FILL,AX

                MOV     XZN,1
                MOV     XZ1C,XZ1L
                MOV     XZ2C,XZ2L

                CMP     YZN,2
                JA      SHORT TED_L14
TED_L15:        DEC     YZ2C
                JNZ     SHORT TED_L14
                INC     YZN
                AND     VIS,0FDH

TED_L14:        MOV     AX,WORD PTR TED_1C
                INC     AX
                CMP     AX,MAX_VER+1            ;V-SYNC
                JB      TED_L17

                CALL    GET_JOYS
                CALL    SYS_KEYS

                INC     BLINK_COUNT
                JNZ     SHORT TED_L18
                MOV     BLINK_COUNT,0F0H
                XOR     VMODE,20H
TED_L18:        XOR     AX,AX
                MOV     BEAM_ADDR,VIDEO_SEG*10000H

                MOV     YZN,2
                MOV     YZ2C,YZ2L
                MOV     CRP,AX
                MOV     CRP_S,AX
                OR      VIS,2
                MOV     BYTE PTR Y_OFFSET +1,40
                MOV     BYTE PTR SVSA +1,40
                TEST    TED_6,10H       ;SCREEN BLANK?
                JZ      SHORT TED_L4
                MOV     BYTE PTR Y_OFFSET +1,AL
                MOV     BYTE PTR SVSA +1,AL

TED_L4:         INC     RETRACE_COUNT   ;RETRACE DEPENDENT CALCULATION
                DEC     SYNCHRO_RETRACE
                JNZ     SHORT TED_L17B
                MOV     SYNCHRO_RETRACE,3
TED_L19:        CMP     AL,SYNCHRO_TIMER
                JE      SHORT TED_L19
                MOV     SYNCHRO_TIMER,AL

TED_L17B:       CMP     AL,SST
                JZ      SHORT TED_L17D
                MOV     WORD PTR TED_1C,4
                INC     SST2
                JMP     SHORT TED_L17A

TED_L17:        CMP     AX,11
                JNZ     SHORT TED_L17D
                CMP     AH,SST2
                JZ      SHORT TED_L17D
                MOV     AL,3
                DEC     SST2
TED_L17D:       MOV     WORD PTR TED_1C,AX
TED_L17A:       MOV     BX, WORD PTR TED_A
                XCHG    BL,BH
                AND     BH,1
                CMP     SST2,0
                JZ      SHORT TED_L12D
                SUB     AL,8
                JB      SHORT TED_L12C
TED_L12D:       CMP     AX,BX           ;RASTER INTERRUPT?
                JNZ     SHORT TED_L12C
                OR      TED_9,82H

TED_L12C:       MOV     BADLICOND,0
                CMP     VSA,7
                JNZ     SHORT TED_L23
                OR      VMODE,40H
                MOV     AX,CRP
                MOV     CRP_S,AX
;                ADD     CRP_S,40
                JMP     SHORT TED_L12A

TED_L23:        INC     VSA
                AND     VSA,7

TED_L12A:       DEC     WORD PTR TED_0  ;TIMERS INTERRUPT?
                JNZ     SHORT TED_L20
                MOV     AX,TIMER1_RELOAD
                MOV     WORD PTR TED_0,AX
                OR      TED_9,88H
TED_L20:        DEC     WORD PTR TED_2
                JNZ     SHORT TED_L21

                OR      TED_9,90H
TED_L21:        DEC     WORD PTR TED_4
                JNZ     SHORT TED_L11

                OR      TED_9,0C0H
TED_L11:        CMP     TICKS,3
                JL      TED
                RETN

T00xxx:         ADD     DI,8
                RETN

T01xxx:         XOR     EAX,EAX
                STOSD
                STOSD
                RETN

T00100:         BTR     DX,11           ;*** MCM+
                JNC     SHORT T01000
                MOVZX   SI,DL
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                MOV     CX,COLOR0               ;CX-0, AX-1, BX-2, DX-3
                MOV     AX,COLOR1
                MOV     BX,COLOR2
                MOV     DL,DH
                JMP     V2PP[ESI*2]

T01000:         MOV     AL,TED_15       ;*** HRV+
T01000_L1:      MOVZX   SI,DL
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                JMP     V1PP[ESI*2]

T00000:         MOV     AL,TED_15       ;***
                MOV     SI,DX           
                AND     SI,7FH
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                OR      DL,DL
                JS      SHORT T11000_L1
                JMP     V1PP[ESI*2]

T11000:         MOV     AL,TED_15       ;*** HRV+ FLASH+
                OR      DH,DH           
                JS      SHORT V1_00
                MOV     SI,CRP
                CMP     SI,WORD PTR TED_C
                JNZ     SHORT T01000_L1
                MOVZX   SI,DL
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
T11000_L1:      XOR     SI,0FFH
                JMP     V1PP[ESI*2]

V1_00:          MOV     AH,AL
                MOV     DX,AX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

T10000:         MOV     AL,TED_15       ;*** FLASH+
                OR      DH,DH           
                JS      SHORT V1_00
                MOV     SI,DX
                AND     SI,7FH
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                MOV     CX,CRP
                CMP     CX,WORD PTR TED_C
                JNZ     SHORT T10000_L1
                XOR     SI,0FFH
T10000_L1:      OR      DL,DL
                JS      SHORT T11000_L1
                JMP     V1PP[ESI*2]

T00010:         MOV     SI,DX           ;*** ECM+
                AND     SI,3FH
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                MOVZX   BX,DL
                SHR     BL,6
                MOV     AL,TED_15[BX]           ;BACKGROUND
                JMP     V1PP[ESI*2]

T00001:         MOV     SI,CRP          ;*** BMM+
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                MOV     AL,DH
                MOV     DH,DL
                MOV     AH,AL
                AND     DX,0FF0H
                AND     AX,7007H
                OR      AL,DL
                OR      DH,AH           ;BACKGROUND
                ROL     AL,4            ;FOREGROUND
                XCHG    AL,DH
                JMP     V1PP[ESI*2]

T00101:         MOV     SI,CRP          ;*** BMM+ MCM+
                MOVZX   SI,GS:[EBX+ESI*8]       ;MATRIX LINE
                MOV     AL,DH
                MOV     DH,DL
                MOV     AH,AL
                AND     DX,0FF0H
                AND     AX,7007H
                OR      AL,DL
                OR      DH,AH           ;COLOR #2
                ROL     AL,4            ;COLOR #1
                MOV     AH,AL
                MOV     DL,DH
                MOV     BX,COLOR1               ;CX-0, AX-1, BX-2, DX-3
                MOV     CX,COLOR0
                XCHG    BX,DX
                JMP     V2PP[ESI*2]

V1_01:          MOV     AH,AL   ;AL-0, DH-1
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AL,DH
                BSWAP   EAX
                STOSD
                RETN

V1_02:          MOV     AH,AL
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AH,DH
                BSWAP   EAX
                STOSD
                RETN

V1_03:          MOV     AH,AL
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AH,DH
                MOV     AL,DH
                BSWAP   EAX
                STOSD
                RETN

V1_04:          MOV     AH,AL
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AH,DH
                STOSD
                RETN

V1_05:          MOV     AH,AL
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AL,DH
                BSWAP   EAX
                MOV     AH,DH
                STOSD
                RETN

V1_06:          MOV     AH,AL
                MOV     DL,AL
                BSWAP   EAX
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                MOV     AH,DH
                BSWAP   EAX
                MOV     AH,DH
                STOSD
                RETN

V1_07:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_08:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_09:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_0A:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_0B:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_0C:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_0D:          MOV     AH,AL
                STOSW
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_0E:          MOV     AH,AL
                STOSW
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_0F:          MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_10:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                RETN

V1_11:          MOV     DL,AL
                SHRD    EAX,EDX,16
                MOV     AL,DL
                MOV     AH,DL
                STOSD
                STOSD
                RETN

V1_12:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_13:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_14:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_15:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_16:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_17:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_18:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_19:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_1A:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_1B:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_1C:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                RETN

V1_1D:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                STOSW
                RETN

V1_1E:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_1F:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_20:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                RETN

V1_21:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_22:          MOV     DL,AL
                XCHG    DL,DH
                SHRD    EAX,EDX,16
                MOV     AL,DH
                MOV     AH,DH
                STOSD
                STOSD
                RETN

V1_23:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_24:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_25:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_26:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_27:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_28:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_29:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_2A:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_2B:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_2C:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                RETN

V1_2D:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                RETN

V1_2E:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,DH
                STOSW
                RETN

V1_2F:          MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_30:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_31:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_32:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_33:          MOV     DL,DH
                ROR     EAX,8
                SHRD    EAX,EDX,16
                MOV     AL,AH
                STOSD
                STOSD
                RETN

V1_34:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_35:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_36:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_37:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_38:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_39:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_3A:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                RETN

V1_3B:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_3C:          MOV     DL,DH
                ROR     EAX,8
                SHRD    EAX,EDX,16
                MOV     AL,AH
                STOSD
                ROL     EAX,16
                STOSD
                RETN

V1_3D:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_3E:          MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_3F:          MOV     AH,AL
                STOSW
                XCHG    AL,DH
                MOV     AH,AL
                STOSW
                STOSW
                STOSW
                RETN

V1_40:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                STOSW
                RETN

V1_41:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_42:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_43:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_44:          MOV     AH,AL
                ROR     EAX,8
                MOV     AH,AL
                ROR     EAX,8
                MOV     AH,DH
                STOSD
                STOSD
                RETN

V1_45:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_46:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_47:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_48:          MOV     AH,AL
                ROR     EAX,8
                MOV     AH,AL
                ROR     EAX,8
                MOV     AH,DH
                STOSD
                XCHG    AL,AH
                STOSD
                RETN

V1_49:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_4A:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_4B:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_4C:          MOV     AH,AL
                ROR     EAX,8
                MOV     AH,AL
                ROR     EAX,8
                MOV     AH,DH
                STOSD
                MOV     AL,AH
                STOSD
                RETN

V1_4D:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_4E:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_4F:          MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_50:          MOV     AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                RETN

V1_51:          MOV     AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_52:          MOV     AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_53:          MOV     AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_54:          MOV     AH,DH
                STOSW
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_55:          MOV     AH,DH
                STOSW
                STOSW
                STOSW
                STOSW
                RETN

V1_56:          MOV     AH,DH
                STOSW
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_57:          MOV     AH,DH
                STOSW
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_58:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_59:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_5A:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_5B:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_5C:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_5D:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_5E:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_5F:          MOV     AH,DH
                STOSW
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_60:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                RETN

V1_61:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_62:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_63:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_64:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_65:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_66:          MOV     AH,AL
                SHL     EAX,16
                MOV     AL,DH
                MOV     AH,DH
                ROL     EAX,8
                STOSD
                STOSD
                RETN

V1_67:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_68:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_69:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_6A:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                RETN

V1_6B:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_6C:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_6D:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_6E:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_6F:          MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                RETN

V1_70:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_71:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_72:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_73:          MOV     DL,AL
                SHRD    EAX,EDX,16
                MOV     DL,DH
                SHRD    EAX,EDX,16
                STOSD
                MOV     AH,AL
                STOSD
                RETN

V1_74:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_75:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                RETN


V1_76:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_77:          MOV     DL,AL
                SHRD    EAX,EDX,16
                MOV     DL,DH
                SHRD    EAX,EDX,16
                STOSD
                STOSD
                RETN

V1_78:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_79:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_7A:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                RETN

V1_7B:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_7C:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_7D:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_7E:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_7F:          MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_80:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                STOSW
                RETN

V1_81:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_82:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_83:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_84:          MOV     DL,AL
                MOV     AH,AL
                SHL     EAX,16
                MOV     AL,DH
                MOV     AH,DL
                STOSD
                ROL     EAX,8
                STOSD
                RETN

V1_85:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_86:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_87:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_88:          MOV     AH,AL
                SHL     EAX,8
                MOV     AL,AH
                SHL     EAX,8
                MOV     AL,DH
                STOSD
                STOSD
                RETN

V1_89:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_8A:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_8B:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_8C:          MOV     AH,AL
                SHL     EAX,8
                MOV     AL,AH
                SHL     EAX,8
                MOV     AL,DH
                STOSD
                MOV     AH,DH
                STOSD
                RETN

V1_8D:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_8E:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_8F:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_90:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                STOSW
                RETN

V1_91:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_92:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_93:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_94:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_95:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                RETN

V1_96:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_97:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_98:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_99:          MOV     AH,AL
                SHL     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                ROR     EAX,8
                STOSD
                STOSD
                RETN

V1_9A:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_9B:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_9C:          MOV     AH,AL
                SHL     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                ROR     EAX,8
                STOSD
                ROL     EAX,8
                STOSD
                RETN

V1_9D:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_9E:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_9F:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,DH
                STOSW
                STOSW
                RETN

V1_A0:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                RETN

V1_A1:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN
                
V1_A2:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_A3:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_A4:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_A5:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_A6:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_A7:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_A8:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_A9:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_AA:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                STOSW
                RETN

V1_AB:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_AC:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_AD:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_AE:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_AF:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                RETN

V1_B0:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_B1:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_B2:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_B3:          MOV     AH,DH
                SHL     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                ROR     EAX,8
                STOSD
                MOV     AL,AH
                STOSD
                RETN

V1_B4:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_B5:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_B6:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_B7:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_B8:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_B9:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_BA:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_BB:          MOV     AH,DH
                SHL     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                ROR     EAX,8
                STOSD
                STOSD
                RETN

V1_BC:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_BD:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_BE:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_BF:          MOV     AH,DH
                XCHG    AL,AH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_C0:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_C1:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_C2:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_C3:          MOV     AH,AL
                SHL     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                STOSD
                ROL     EAX,16
                STOSD
                RETN

V1_C4:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_C5:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_C6:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_C7:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_C8:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_C9:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_CA:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_CB:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_CC:          MOV     AH,AL
                SHL     EAX,16  ;BSWAP
                MOV     AH,DH
                MOV     AL,DH
                STOSD
                STOSD
                RETN

V1_CD:          MOV     AH,DH
                XCHG    AL,DH
                MOV     DL,DH
                STOSW
                XCHG    AX,DX
                STOSW
                XCHG    AX,DX
                STOSW
                MOV     AL,DL
                STOSW
                RETN

V1_CE:          MOV     AH,DH
                XCHG    AL,DH
                MOV     DL,DH
                STOSW
                XCHG    AX,DX
                STOSW
                XCHG    AX,DX
                STOSW
                MOV     AH,DL
                STOSW
                RETN

V1_CF:          MOV     AH,DH
                XCHG    AL,DH
                MOV     DL,DH
                STOSW
                XCHG    AX,DX
                STOSW
                XCHG    AX,DX
                STOSW
                STOSW
                RETN

V1_D0:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_D1:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_D2:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_D3:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_D4:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_D5:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_D6:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_D7:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_D8:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_D9:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_DA:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                STOSW
                RETN

V1_DB:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_DC:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_DD:          MOV     AH,DH
                SHL     EAX,16
                MOV     AL,DH
                MOV     AH,DH
                STOSD
                STOSD
                RETN

V1_DE:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_DF:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                RETN

V1_E0:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,AH
                STOSW
                STOSW
                RETN

V1_E1:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_E2:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_E3:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,AH
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_E4:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,AL
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_E5:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,AL
                STOSW
                STOSW
                RETN

V1_E6:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,AL
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_E7:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,AL
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_E8:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_E9:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_EA:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                STOSW
                RETN

V1_EB:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_EC:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                RETN

V1_ED:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_EE:          MOV     AH,DH
                ROR     EAX,16
                MOV     AH,DH
                MOV     AL,DH
                ROL     EAX,8
                STOSD
                STOSD
                RETN

V1_EF:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                XCHG    AH,DH
                STOSW
                XCHG    AH,DH
                STOSW
                STOSW
                RETN

V1_F0:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_F1:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_F2:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                XCHG    AL,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_F3:          MOV     AH,DH
                XCHG    AL,DH
                MOV     DL,DH
                STOSW
                STOSW
                XCHG    AX,DX
                STOSW
                MOV     AX,DX
                STOSW
                RETN

V1_F4:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_F5:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                STOSW
                RETN

V1_F6:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_F7:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                MOV     AL,AH
                STOSW
                RETN

V1_F8:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_F9:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                XCHG    AL,AH
                STOSW
                RETN

V1_FA:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                STOSW
                RETN

V1_FB:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                MOV     AH,AL
                STOSW
                RETN

V1_FC:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                STOSW
                MOV     AL,DH
                MOV     AH,DH
                STOSW
                RETN

V1_FD:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                STOSW
                MOV     AL,DH
                STOSW
                RETN

V1_FE:          MOV     AH,DH
                XCHG    AL,DH
                STOSW
                STOSW
                STOSW
                MOV     AH,DH
                STOSW
                RETN

V1_FF:          MOV     AL,DH
                MOV     AH,DH
                MOV     DL,AL
                SHL     EAX,16
                MOV     AX,DX
                STOSD
                STOSD
                RETN

V2_00:          MOV     AX,CX   ;CX-0, AX-1, BX-2, DX-3
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                STOSD
                RETN

V2_01:          MOV     BX,AX
V2_02:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_03:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_04:          MOV     DX,AX
V2_0C:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_05:          MOV     DX,AX
V2_0F:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_06:          MOV     DX,AX
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_07:          MOV     BX,AX
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_08:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_09:          MOV     DX,AX
V2_0B:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_0A:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_0D:          MOV     BX,CX
V2_0E:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_10:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_12:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_13:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_14:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_15:          MOV     DX,AX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_18:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_1C:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_20:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_21:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_22:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                STOSD
                RETN

V2_23:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_24:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_25:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_26:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_27:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_28:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_29:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_2A:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_2B:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_2C:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_2D:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_2E:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_2F:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_30:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_31:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_32:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_33:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                STOSD
                RETN

V2_34:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_35:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_36:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_37:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_38:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_39:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_3A:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_3B:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_3C:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_3D:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_3E:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_3F:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_40:          MOV     BX,CX
V2_48:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_41:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_42:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_43:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_44:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                STOSD
                RETN

V2_45:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_46:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_47:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_49:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_4A:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_4B:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_4C:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_4D:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_4E:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_4F:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_50:          MOV     DX,AX
V2_D0:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_51:          MOV     BX,AX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_52:          MOV     DX,AX
V2_D2:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_53:          MOV     BX,AX
V2_93:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_54:          MOV     DX,AX
V2_D4:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_55:          MOV     CX,AX
V2_11:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                STOSD
                RETN

V2_56:          MOV     CX,AX
V2_16:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_57:          MOV     CX,AX
V2_17:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_58:          MOV     DX,AX
V2_D8:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_59:          MOV     CX,AX
V2_19:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_5A:          MOV     CX,AX
V2_1A:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_5B:          MOV     CX,AX
V2_1B:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_5C:          MOV     BX,AX
V2_9C:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_5D:          MOV     CX,AX
V2_1D:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_5E:          MOV     CX,AX
V2_1E:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_5F:          MOV     CX,AX
V2_1F:          BSWAP   EAX
                MOV     AX,CX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_60:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_61:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_63:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_64:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_65:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_66:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                STOSD
                RETN

V2_67:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_68:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_69:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_6A:          MOV     CX,BX
V2_62:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_6B:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_6C:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_6D:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_6E:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_6F:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_70:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_71:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_72:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_73:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_74:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_75:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_76:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_77:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

V2_78:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_79:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_7A:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_7B:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_7C:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_7D:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_7E:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_7F:          BSWAP   EAX
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_80:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_81:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_82:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_83:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_84:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_85:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_86:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_87:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_88:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                STOSD
                RETN

V2_89:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_8A:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_8B:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_8C:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_8D:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_8E:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_8F:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_90:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_92:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_94:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_95:          MOV     CX,AX
V2_91:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_96:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_97:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_98:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_99:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                STOSD
                RETN

V2_9A:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_9B:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_9D:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_9E:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_9F:          BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_A1:          MOV     DX,AX
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_A4:          MOV     DX,AX
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_A5:          MOV     CX,AX
V2_A0:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_A6:          MOV     CX,AX
V2_A2:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_A7:          MOV     CX,AX
V2_A3:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_A9:          MOV     CX,AX
V2_A8:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_AA:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                STOSD
                RETN

V2_AB:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_AD:          MOV     CX,AX
V2_AC:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_AE:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_AF:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_B0:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_B1:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_B2:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_B3:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_B4:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_B5:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_B6:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_B7:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_B9:          MOV     CX,AX
V2_B8:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_BA:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_BB:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                STOSD
                RETN

V2_BC:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_BD:          XCHG    AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                BSWAP   EAX
                STOSD
                RETN

V2_BE:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_BF:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                MOV     AX,DX
                STOSD
                RETN

V2_C0:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_C1:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_C2:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_C3:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_C4:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_C5:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_C6:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_C7:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_C8:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_C9:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_CA:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_CB:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_CC:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

V2_CD:          XCHG    AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_CE:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_CF:          MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_D3:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_D5:          MOV     CX,AX
V2_D1:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_D6:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_D7:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_D9:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_DA:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_DB:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_DC:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_DD:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

V2_DE:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_DF:          BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_E0:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_E1:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_E2:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_E4:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_E5:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_E6:          XCHG    AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_E7:          MOV     CX,AX
V2_E3:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_E9:          MOV     CX,AX
V2_E8:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_EA:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_EB:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                STOSD
                RETN

V2_ED:          MOV     CX,AX
V2_EC:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_EE:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

V2_EF:          MOV     AX,BX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                RETN

V2_F0:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_F1:          MOV     BX,AX
V2_F2:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_F3:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_F4:          MOV     BX,AX
V2_F8:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_F5:          MOV     CX,AX
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_F6:          MOV     CX,AX
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,CX
                STOSD
                RETN

V2_F7:          MOV     CX,AX
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                STOSD
                RETN

V2_F9:          MOV     CX,AX
                MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_FA:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                MOV     AX,BX
                STOSD
                RETN

V2_FB:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                STOSD
                RETN

V2_FD:          MOV     CX,AX
V2_FC:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,CX
                BSWAP   EAX
                STOSD
                RETN

V2_FE:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                MOV     AX,BX
                BSWAP   EAX
                STOSD
                RETN

V2_FF:          MOV     AX,DX
                BSWAP   EAX
                MOV     AX,DX
                STOSD
                STOSD
                RETN

